# NIDS ML — Environment Variable Template
# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# ── Model serving ─────────────────────────────────────────────────────────────
# Path to the production model directory (must contain tier1_rf.pkl etc.)
MODEL_DIR=models/production/v1.0.0

# API server settings
API_HOST=0.0.0.0
API_PORT=8000

# ── Logging ───────────────────────────────────────────────────────────────────
# Logging level: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# ── MLflow (optional – leave empty to disable tracking) ───────────────────────
# MLFLOW_TRACKING_URI=http://localhost:5000
# MLFLOW_EXPERIMENT_NAME=nids-hybrid

# ── Dataset paths (override defaults in configs/datasets/*.yaml) ──────────────
# DATA_DIR=data/raw
