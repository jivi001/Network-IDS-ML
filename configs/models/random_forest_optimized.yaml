# Optimized Random Forest Model Configuration

model_type: random_forest
description: Optimized Tier 1 classifier

hyperparameters:
  n_estimators: 200
  max_depth: 20
  min_samples_split: 10 # Tuned
  min_samples_leaf: 1 # Tuned
  max_features: sqrt
  criterion: gini
  class_weight: null # Tuned (None, because SMOTE balances data)
  random_state: 42
  n_jobs: -1
  bootstrap: true
  oob_score: false
  verbose: 0
