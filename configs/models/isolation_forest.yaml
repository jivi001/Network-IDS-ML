# Isolation Forest Model Configuration

model_type: isolation_forest
description: Tier 2 unsupervised anomaly detector for zero-day attacks

hyperparameters:
  contamination: 0.05
  n_estimators: 200
  max_samples: auto
  max_features: 1.0
  bootstrap: false
  random_state: 42
  n_jobs: -1
  verbose: 0
